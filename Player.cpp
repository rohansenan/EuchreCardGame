#include <iostream>
#include "Player.h"
#include "Pack.h"
#include "Card.h"

using namespace std;

Player * Player_factory(const std::string &name, const std::string &strategy)
{
    if (strategy == "simple")
    {
        
    }

}